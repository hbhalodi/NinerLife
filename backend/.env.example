# Leave DATABASE_URL unset for the default local SQLite database.
# In Render, set this to the private PostgreSQL URL shown by your database.
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DATABASE

# The exact deployed frontend origin allowed to call the API. Do not include a path.
# FRONTEND_ORIGIN=https://your-project.vercel.app
